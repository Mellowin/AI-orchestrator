{
  "id": "exit-code",
  "category": "fixable",
  "classification": "WRONG_FUNCTION_BEHAVIOR",
  "fixable": true,
  "repair_strategy": "apply_fix_patch",
  "allowed_files": ["src/reliability-fixtures/exit-codes.ts"],
  "setup": [
    {
      "path": "src/reliability-fixtures/exit-codes.ts",
      "search": "return status === 0 ? 0 : 1;",
      "replace": "return status;"
    }
  ],
  "fix": [
    {
      "path": "src/reliability-fixtures/exit-codes.ts",
      "search": "return status;",
      "replace": "return status === 0 ? 0 : 1;"
    }
  ],
  "reproduction_command": ["npx", "tsx", "--test", "test/fixtures/reliability-scenarios/exit-code.test.ts"],
  "verification_commands": [
    ["npx", "tsx", "--test", "test/fixtures/reliability-scenarios/exit-code.test.ts"]
  ],
  "expected_verdict": "REPAIRED"
}
