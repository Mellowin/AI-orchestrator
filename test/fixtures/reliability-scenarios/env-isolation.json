{
  "id": "env-isolation",
  "category": "fixable",
  "classification": "WINDOWS_PATH_OR_ENV_ISOLATION_FAILURE",
  "fixable": true,
  "repair_strategy": "apply_fix_patch",
  "allowed_files": ["src/reliability-fixtures/env-runner.ts"],
  "setup": [
    {
      "path": "src/reliability-fixtures/env-runner.ts",
      "search": "delete sanitized.SECRET_VAR;",
      "replace": "// SECRET_VAR leak injected"
    }
  ],
  "fix": [
    {
      "path": "src/reliability-fixtures/env-runner.ts",
      "search": "// SECRET_VAR leak injected",
      "replace": "delete sanitized.SECRET_VAR;"
    }
  ],
  "reproduction_command": ["npx", "tsx", "--test", "test/fixtures/reliability-scenarios/env-isolation.test.ts"],
  "verification_commands": [
    ["npx", "tsx", "--test", "test/fixtures/reliability-scenarios/env-isolation.test.ts"]
  ],
  "expected_verdict": "REPAIRED"
}
