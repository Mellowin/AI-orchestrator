{
  "name": "ai-orchestrator",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "tsx --test",
    "demo:block:fake": "tsx scripts/demo-real-block-run-fake.ts",
    "verify:product": "npm run typecheck && npm run build && npm test && npm run demo:block:fake"
  },
  "dependencies": {
    "cross-spawn": "^7.0.6",
    "dotenv": "^16.4.5",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/cross-spawn": "^6.0.6",
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
